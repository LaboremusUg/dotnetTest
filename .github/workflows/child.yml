name: Test (Triggered/Manual)

on:
  push:
    branches:
      - "main"
  workflow_dispatch:
  
jobs:
  Build-Package-IIS_Deploy:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET 🎇
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 5.0.x
          
      - name: Restore ⚒
        run: dotnet restore ConsoleApp1/ConsoleApp1.csproj
                 
      - name: Build 🚀
        run: dotnet build ConsoleApp1/ConsoleApp1.csproj --no-restore

      - name: Test 🧪
        run: dotnet test TestProject1/TestProject1.csproj --collect:"XPlat Code Coverage" --results-directory "TestResults"
        #run: dotnet test TestProject1/TestProject1.csproj --logger trx --results-directory "TestResults" -c Release

      # - name: Upload unit test results 📚
      #   uses: actions/upload-artifact@v2.3.1
      #   if: ${{ always() }}
      #   with:
      #     name: Unit-test-results
      #     path: TestResults

      # - name: Get coverage file path
      #   run: $sas=Get-ChildItem -Filter coverage.cobertura.xml -Recurse | %{$_.FullName}

      - name: Upload coverage to Codecov 
        uses: codecov/codecov-action@v1
        if: ${{ always() }}
        # with:
        #   file: ./TestProject1/TestResults/**/coverage.cobertura.xml
          # name: codecov
          # token: ${{ secrets.CODECOV }}

